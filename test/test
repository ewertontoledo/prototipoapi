const request = require("supertest");
const app = require("../app");

describe("GET /", () => {
   it("Protótipo API Teste", (done) => {
      request(app).get("/").expect({titulo: "Protótipo API"}, done);
   })
});
